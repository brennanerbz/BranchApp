import React, { Component, PropTypes } from 'react';

export default class UserInfo extends Component {
	static propTypes = {
	}

	render() {
		return(
			<div id="user_info">
				<h2>User info</h2>
			</div>
		);
	}
}