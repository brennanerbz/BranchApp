import React, { Component, PropTypes } from 'react';

export default class ChatHeader extends Component {
	static propTypes = {
	}

	render() {
		return(
			<div>
				<h1>Chat header</h1>
			</div>
		);
	}
}