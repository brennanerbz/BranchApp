import React, { Component, PropTypes } from 'react';

export default class FeedHeader extends Component {
	static propTypes = {
	}

	render() {
		return(
			<div id="feed_header">
				<h2>Feed header</h2>
			</div>
		);
	}
}