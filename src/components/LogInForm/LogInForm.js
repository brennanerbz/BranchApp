import React, { Component, PropTypes } from 'react';

export default class LogInForm extends Component {
	static propTypes = {
	}

	render() {
		const style = require('./LogInForm.scss')
		return (
			<div className={style.log_in_form}>
			</div>
		);
	}
}
