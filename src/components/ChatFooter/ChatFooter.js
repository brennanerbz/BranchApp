import React, { Component, PropTypes } from 'react';

export default class ChatFooter extends Component {
	static propTypes = {
	}

	render() {
		return(
			<div>
				<h1>Chat footer</h1>
			</div>
		);
	}
}